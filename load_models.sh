#!/bin/sh

# This script has been deprecated as we're now using OpenAI embeddings
# instead of PostgresML for the lightweight MVP version

echo "CommandCore is now using OpenAI embeddings via API."
echo "No local models need to be loaded."
echo "The system is ready to use."
